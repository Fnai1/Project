# 1C CI/CD Pipeline

🚀 Автоматизированный CI/CD пайплайн для сборки, тестирования и развертывания конфигураций 1С:Предприятие.

## 📚 Содержание

- [Возможности](#-возможности)
- [Быстрый старт](#-быстрый-старт)
- [Настройка секретов](#-настройка-секретов)
- [Использование](#-использование)
- [Структура проекта](#-структура-проекта)
- [FAQ](#-faq)

## ✨ Возможности

- 🔨 **Автоматическая сборка** конфигураций из хранилища 1С
- 📦 **Создание резервных копий** перед каждым деплоем
- 🚀 **Автоматический деплой** на тестовый сервер
- 🧪 **Smoke-тестирование** после развертывания
- 📊 **Детальное логирование** всех операций
- 🔔 **Уведомления** команды о результатах
- 🎭 **Mock-режим** для демонстрации без реального подключения к 1С

## 🚀 Быстрый старт

### Вариант 1: Демонстрационный режим (без настройки секретов)

Для демонстрации работы пайплайна используйте **mock-версию**:

1. Fork этого репозитория
2. Перейдите в **Actions** → выберите **"1C CI/CD Pipeline (Mock Demo)"**
3. Нажмите **"Run workflow"**
4. Введите имя конфигурации (например, `test-config`)
5. Запустите workflow

✅ Mock-версия работает **сразу без настройки** и демонстрирует весь процесс CI/CD!

### Вариант 2: Реальная работа с 1С (требуется настройка)

Для работы с реальным хранилищем 1С:

1. Fork этого репозитория
2. Настройте [секреты GitHub](#-настройка-секретов)
3. Используйте workflow **"1C CI/CD Pipeline"**

## 🔐 Настройка секретов

### Где добавить секреты

1. Перейдите в **ваш форк** репозитория
2. Откройте **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте необходимые секреты

### Обязательные секреты

#### Для сборки конфигурации

| Имя секрета | Описание | Пример значения |
|------------|----------|----------------|
| `ONEC_PLATFORM` | Путь к платформе 1С | `C:\Program Files\1cv8\8.3.25.1257` |
| `STORAGE` | Путь к хранилищу конфигурации | `tcp://server:1542/storage` или `\\\\server\\1cv8ddb` |
| `STORAGE_USER` | Имя пользователя хранилища | `developer` |
| `STORAGE_PWD` | Пароль пользователя хранилища | `SecurePassword123` |

#### Для деплоя на тестовый сервер

| Имя секрета | Описание | Пример значения |
|------------|----------|----------------|
| `TEST_IB_CONN` | Строка подключения к тестовой ИБ | `/F"C:\\InfoBases\\Test"` или `/S"server\\testbase"` |
| `TEST_IB_USER` | Логин администратора ИБ | `Admin` |
| `TEST_IB_PWD` | Пароль администратора ИБ | `admin123` |

#### Опциональные секреты

| Имя секрета | Описание | Пример значения |
|------------|----------|----------------|
| `PUBLISH_URL` | URL для публикации релизов | `https://releases.company.com` |

### Примеры значений для разных сценариев

#### Файловая информационная база
```
TEST_IB_CONN=/F"C:\InfoBases\TestBase"
```

#### Серверная информационная база
```
TEST_IB_CONN=/S"1c-server\testbase"
```

#### Файловое хранилище конфигурации
```
STORAGE=\\\\fileserver\\share\\1cv8\\storage
```

#### Серверное хранилище (TCP)
```
STORAGE=tcp://1c-server:1542/storage_name
```

### 🔒 Безопасность секретов

- ✅ Секреты **зашифрованы** в GitHub и не видны в логах
- ✅ Секреты доступны **только в вашем репозитории**
- ✅ Секреты **не копируются** при форке
- ⚠️ **Никогда не коммитьте** пароли в код!

## 📖 Использование

### Автоматический запуск

Пайплайн запускается автоматически при:

- **Push** в ветки `main` или `develop`
- Создании **Pull Request** в ветку `main`

### Ручной запуск

1. Перейдите в **Actions**
2. Выберите нужный workflow:
   - `1C CI/CD Pipeline` - реальная работа с 1С
   - `1C CI/CD Pipeline (Mock Demo)` - демонстрация
3. Нажмите **"Run workflow"**
4. Выберите ветку
5. Введите имя конфигурации (опционально)
6. Нажмите **"Run workflow"**

### Локальный запуск скриптов

Можно запускать скрипты локально без GitHub Actions:

```powershell
# Сборка конфигурации
pwsh -File scripts/build-1c.ps1 `
  -Platform "C:\Program Files\1cv8\8.3.25.1257" `
  -Storage "\\\\server\\storage" `
  -StorageUser "developer" `
  -StoragePwd "password" `
  -OutCf "output/config.cf"

# Деплой конфигурации
pwsh -File scripts/deploy-1c.ps1 `
  -Platform "C:\Program Files\1cv8\8.3.25.1257" `
  -Infobase '/F"C:\InfoBases\Test"' `
  -IBUser "Admin" `
  -IBPwd "" `
  -Cf "output/config.cf"

# Smoke-тест
pwsh -File tests/smoke.ps1 `
  -Platform "C:\Program Files\1cv8\8.3.25.1257" `
  -Infobase '/F"C:\InfoBases\Test"' `
  -IBUser "Admin" `
  -IBPwd ""
```

## 📁 Структура проекта

```
Project/
├── .github/
│   └── workflows/
│       ├── main.yml              # Основной CI/CD пайплайн
│       └── mock-pipeline.yml     # Demo-версия без 1С
├── scripts/
│   ├── build-1c.ps1             # Сборка конфигурации из хранилища
│   ├── deploy-1c.ps1            # Деплой на сервер
│   ├── rollback-1c.ps1          # Откат конфигурации
│   └── simple-build.ps1         # Создание резервной копии
├── tests/
│   └── smoke.ps1                # Smoke-тесты
├── artifacts/                    # Артефакты сборки (генерируется)
│   ├── build/                   # CF-файлы
│   ├── backups/                 # Резервные копии
│   └── logs/                    # Логи
├── configs/                      # Конфигурационные файлы
├── init-1c-scripts.ps1          # Генератор скриптов
└── README.md                     # Эта документация
```

## 🔄 Процесс CI/CD

### Этап 1: Сборка (Build)

1. **Подготовка окружения**
   - Создание структуры каталогов
   - Проверка платформы 1С

2. **Получение конфигурации**
   - Подключение к хранилищу 1С
   - Обновление из последней версии
   - Выгрузка в CF-файл

3. **Создание резервной копии**
   - Сохранение CF с временной меткой
   - Архивация артефактов

### Этап 2: Тест и деплой (Test & Deploy)

1. **Проверка целостности**
   - Валидация CF-файла
   - Проверка метаданных

2. **Развертывание**
   - Подключение к тестовой ИБ
   - Загрузка конфигурации
   - Обновление структуры БД

3. **Тестирование**
   - Smoke-тесты
   - Проверка базовых операций

4. **Отчетность**
   - Генерация логов
   - Сохранение артефактов
   - Уведомление команды

## ❓ FAQ

### Как обновить скрипты после клонирования?

Запустите `init-1c-scripts.ps1` для пересоздания скриптов:

```powershell
pwsh -File init-1c-scripts.ps1
```

### Можно ли использовать без GitHub Actions?

Да! Все скрипты в папке `scripts/` можно запускать локально через PowerShell.

### Как добавить дополнительные тесты?

Создайте новые PowerShell скрипты в папке `tests/` и добавьте их в workflow.

### Поддерживается ли Linux?

Мock-версия работает на Linux. Для реальной работы нужен Windows runner с установленной платформой 1С.

### Как настроить уведомления в Slack?

Добавьте секрет `SLACK_WEBHOOK_URL` и используйте action:

```yaml
- name: Slack Notification
  uses: slackapi/slack-github-action@v1
  with:
    webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
    payload: |
      {
        "text": "Деплой ${{ env.CONFIG_NAME }} завершен!"
      }
```

### Как добавить email уведомления?

Используйте action для отправки email:

```yaml
- name: Send Email
  uses: dawidd6/action-send-mail@v3
  with:
    server_address: smtp.gmail.com
    server_port: 465
    username: ${{ secrets.EMAIL_USERNAME }}
    password: ${{ secrets.EMAIL_PASSWORD }}
    subject: Деплой ${{ env.CONFIG_NAME }}
    to: team@company.com
    from: ci-cd@company.com
    body: Конфигурация успешно развернута!
```

## 📝 Лицензия

MIT License - используйте свободно для своих проектов!

## 🤝 Вклад в проект

Приветствуются Pull Requests с улучшениями и исправлениями!

## 📧 Контакты

Если возникли вопросы - создайте Issue в репозитории.

---

**Создано для упрощения CI/CD процессов с 1С:Предприятие** 🚀
